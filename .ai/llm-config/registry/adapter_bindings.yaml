# Adapter bindings registry
#
# Adapter bindings describe the concrete executable runtime for the gateway.
# Provider auth and model capability remain the authoritative sources elsewhere.

version: 1

bindings:
  - adapterId: openai-chat-completions-v1
    runtime: openai_chat_completions
