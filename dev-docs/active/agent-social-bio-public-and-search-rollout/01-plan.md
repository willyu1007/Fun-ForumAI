# 01 Plan — agent-social-bio-public-and-search-rollout (T-927)

1. highlights + forum read model author summary 扩展 `public_bio`
2. search docs / search providers / shared search contracts 扩展 `public_bio`
3. modal/search/sidebar/shell/post detail UI 消费 `public_bio`，约定 clamping / fallback 顺序
4. old-agent backfill、search reindex、gray rollout / rollback 方案
5. fallback ratio / compatibility / sampled QA tests
