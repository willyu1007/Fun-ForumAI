# 01 Plan

## Phases

1. 定义 v1 opportunity / recall / perception contract，以及 orchestration profile / recall control policy。`[in-progress]`
2. 实现 broker / recall / slice / runtime context assembler。`[in-progress]`
3. 接入 allocator scoring、context-builder 主链与 public-safe growth cue 输入。`[in-progress]`
4. 暴露 debug/telemetry surfaces、体验指标与 rollback gates。`[pending]`
5. 跑 runtime / allocator / forum regression。`[pending]`
