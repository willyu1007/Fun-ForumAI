# 03 Implementation Notes — T-986

- 2026-04-21: Verified Step 3 root rules, Step 2 canonical seed/review truth, and idle live-run markers before creating any new Step 3 workspace.
- 2026-04-21: Initialized Step 3 live run `kickoff-step3-20260421-01`, froze the current seed and planning review into run-local snapshots, created the initial `01-run-state.yaml`, and derived `wave-01/00-wave-plan.yaml` from the repo’s `mixed_clusters_then_escalate` selection logic rather than from historical sample artifacts.
- 2026-04-21: Wrote `wave-01/01-director-note.md` for the new live run and advanced `01-run-state.yaml` from `director` to `runtime_guard`, preserving Step 3 role order and keeping the generation boundary at the director-intent layer only.
- 2026-04-21: Wrote `wave-01/02-runtime-guardrails.yaml` for the live run, carrying forward all slot-level unresolved boundaries, entity requirements, actor source policy, and `kickoff_role_hints` from the frozen seed into machine-readable runtime constraints. Advanced `01-run-state.yaml` from `runtime_guard` to `writer`.
- 2026-04-21: Wrote `wave-01/03-slot-content-units.yaml` with 7 passed slot content units, each aligned to the frozen seed, current director note, and current runtime guardrails. Advanced `01-run-state.yaml` from `writer` to `visual`.
- 2026-04-21: Wrote `wave-01/04-visual-units.yaml` with 7 visual units and created the referenced local SVG assets under `waves/wave-01/assets/`. Verified every `attachment_payload.relative_path` exists locally, then advanced `01-run-state.yaml` from `visual` to `wave_checkpoint`.
- 2026-04-21: Tightened the Step 3 protocol and templates to remove the semantic drift between “attachment-ready shape” and “real slot visual.” Updated `STEP-3-RULES.md`, `model-consumption-protocol.md`, Step 3 templates, the live run SOP copy, and high-level kickoff docs so that live runs now require semantically complete final visuals instead of sample/reference title cards.
- 2026-04-21: Replaced the live run’s `wave-01` SVG placeholder cards with 7 context-specific raster images generated via the built-in `image_gen` workflow, copied the selected PNG finals into `waves/wave-01/assets/`, and rewrote `wave-01/04-visual-units.yaml` to point at those PNG assets with explicit `not_placeholder_visual` quality-gate checks.
- 2026-04-21: After visual review found the first raster pass too similar across multiple slots, regenerated four visuals with deliberately separated visual grammars: `emotion-jury-01` to an intimate backstage boundary scene, `values-stage-01` to a top-down civic forum split, `fail-postmortem-01` to a messy control-room incident aftermath, and `weekly-headline-01` to a cool newsroom flat-lay briefing view. Updated `wave-01/04-visual-units.yaml` to point to the selected `-v2.png` finals.
- 2026-04-21: Wrote `wave-01/05-wave-checkpoint.yaml` and closed `wave-01` with `recommended_action=continue_wave_generation`. Recorded one non-blocking drift signal about early visual-family convergence so `wave-02` planning can bias toward more diverse camera/composition families from the start. Updated `01-run-state.yaml` to `current_role=idle`, `next_expected_action=prepare_wave_plan`, `current_wave_status=closed`, `completed_wave_count=1`, and `remaining_wave_count=5`.
- 2026-04-21: Assembled the current run-level Step 3 package from the closed `wave-01` outputs. Generated `package/00-content-package.yaml` and derived `package/01-review-snapshot.yaml`, both scoped to the currently passed wave set only. Refreshed `01-run-state.yaml` so the run now has a real partial assembled package while still remaining `in_progress` and not yet `ready_for_editorial_review`.
- 2026-04-21: Patched the Step 3 documentation to remove the package-assembly semantic drift. `STEP-3-RULES.md`, `model-consumption-protocol.md`, `state-transition-table.md`, the SOP template, the live-run SOP copy, and `kickoff-single-flow.md` now explicitly say that after every passing wave checkpoint, the single cumulative Step 3 package and derived review snapshot must be refreshed before the next wave or editorial handoff.
- 2026-04-21: Started `wave-02` by deriving and writing `waves/wave-02/00-wave-plan.yaml` from the repo selection logic plus the `wave-01` drift bias. Updated `01-run-state.yaml` to `current_wave_id=wave-02`, `current_role=director`, `current_wave_status=planned`, and the new selected slot scope.
- 2026-04-21: Wrote `wave-02/01-director-note.md` with explicit broadening goals across creator, night, programming, clue, evidence, and responsibility lanes, and pushed the prior visual-diversity drift into director-level constraints. Updated `01-run-state.yaml` from `director` to `runtime_guard` with `current_wave_status=director_done`.
- 2026-04-21: Wrote `wave-02/02-runtime-guardrails.yaml` for the new wave, carrying forward slot-level unresolved rules, entity requirements, actor source policy, and role hints, and concretizing the director’s visual-diversity requirement into media-level family constraints for the media-required slots. Updated `01-run-state.yaml` from `runtime_guard` to `writer` with `current_wave_status=runtime_guard_done`.
- 2026-04-22: Wrote `wave-02/03-slot-content-units.yaml` with 7 passed writer units spanning meme probe, relationship boundary, late-night aftershock, clue entry, programming pressure, evidence splitting, and responsibility splitting. Bound media-required slots to future visual refs, kept `late-night-radio-01` and `emotion-jury-02` text-first, and advanced `01-run-state.yaml` from `writer` to `visual` with `current_wave_status=visual_in_progress`.
- 2026-04-22: Generated 5 real raster finals for `wave-02` media-required slots, copied the selected PNGs into `waves/wave-02/assets/`, and wrote `wave-02/04-visual-units.yaml` with slot-specific visual families for public reaction slice, artifact boundary board, clue entry portal, schedule surface, and fragment compare. Regenerated `banter-watch-01` and `hot-arena-02` once each to reduce semantic drift and single-reading bias, then advanced `01-run-state.yaml` from `visual` to `wave_checkpoint` with `current_wave_status=checkpoint_pending`.
- 2026-04-22: After user review flagged too much visual-family convergence inside `wave-02`, regenerated `creator-relationship-01`, `limited-program-01`, and `hot-arena-02` again to separate them into a glass-partition boundary scene, a corridor wall scheduling scene, and a public-device fragment-compare scene. Updated `wave-02/04-visual-units.yaml` to reflect the new selected finals while keeping the run at `wave_checkpoint` pending state because checkpoint execution had not started yet.
- 2026-04-22: Wrote `wave-02/05-wave-checkpoint.yaml` and marked the wave as checkpoint-passed with one non-blocking visual calibration drift signal. Updated `01-run-state.yaml` from `wave_checkpoint` to `idle`, set `next_expected_action=assemble_content_package`, cleared pending slot ids, and advanced `current_wave_status=checkpoint_passed` so cumulative package assembly can run before wave closure/next-wave planning.
- 2026-04-22: Refreshed the cumulative Step 3 package from `wave-01` plus `wave-02`, regenerated `package/00-content-package.yaml` and `package/01-review-snapshot.yaml`, and then closed `wave-02` in `01-run-state.yaml`. The run now has `completed_wave_count=2`, `remaining_wave_count=4`, `current_wave_status=closed`, and `next_expected_action=prepare_wave_plan`.
- 2026-04-22: Derived `wave-03/00-wave-plan.yaml` from the remaining `slot_brief_set` entries using the repo’s `mixed_clusters_then_escalate` selection logic rather than from sample artifacts. The selected `wave-03` scope stays inside the full `sequence_index=2` layer and covers 7 communities across `evidence_reading`, `value_conflict`, and `public_signal`, while folding the latest visual calibration drift into the wave objective. Updated `01-run-state.yaml` to `current_wave_id=wave-03`, `current_role=director`, `current_wave_status=planned`, and the new pending slot scope.
- 2026-04-22: Wrote `wave-03/01-director-note.md` to make the wave’s escalation axis explicit: this batch should harden the disagreement around competing interpretations of the same public facts, not hunt for a single bigger reveal. The note also pre-allocates distinct camera families for `persona-chaos-02`, `fail-postmortem-02`, and `weekly-headline-02` so visual drift is corrected before generation rather than during checkpoint cleanup. Updated `01-run-state.yaml` from `director` to `runtime_guard` with `current_wave_status=director_done`.
- 2026-04-22: Wrote `wave-03/02-runtime-guardrails.yaml`, carrying the current wave’s unresolved boundaries, entity requirements, actor-source policy, and role-hint translations into machine-readable writer/visual constraints. This pass also formalizes three separated media families plus per-slot emotional floors for `persona-chaos-02`, `fail-postmortem-02`, and `weekly-headline-02`, so the next visual step cannot collapse back into board/desk/analysis-wall convergence. Updated `01-run-state.yaml` from `runtime_guard` to `writer` with `current_wave_status=runtime_guard_done`.
- 2026-04-22: Wrote `wave-03/03-slot-content-units.yaml` with 7 passed writer units spanning clip-interpretation divergence, rule-order conflict, creator-side echo, process-gap escalation, restrained fact stack, joke-as-alignment split, and intimate dilemma. Bound the 3 media-required slots to future visual refs while keeping the remaining 4 slots text-first, and advanced `01-run-state.yaml` from `writer` to `visual` with `current_wave_status=visual_in_progress`.
- 2026-04-22: Generated 3 final raster visuals for `wave-03` media-required slots via the built-in `image_gen` workflow, copied the selected PNGs into `waves/wave-03/assets/`, and wrote `wave-03/04-visual-units.yaml`. The chosen finals preserve the pre-locked family split: `persona-chaos-02` as reflective screen / clip-fracture, `fail-postmortem-02` as process choke-point corridor, and `weekly-headline-02` as restrained newsroom bulletin surface. Updated `01-run-state.yaml` from `visual` to `wave_checkpoint` with `current_wave_status=checkpoint_pending`.
- 2026-04-22: Wrote `wave-03/05-wave-checkpoint.yaml` and marked the wave as checkpoint-passed with no hard failures and no residual drift signals. Updated `01-run-state.yaml` from `wave_checkpoint` to `idle`, cleared pending slot ids, and set `next_expected_action=assemble_content_package` so the cumulative package can be refreshed before `wave-03` is formally closed.
- 2026-04-22: Refreshed the cumulative Step 3 package from `wave-01` through `wave-03`, regenerated `package/00-content-package.yaml` and `package/01-review-snapshot.yaml`, and then closed `wave-03` in `01-run-state.yaml`. The run now has `completed_wave_count=3`, `remaining_wave_count=3`, `current_wave_status=closed`, and `next_expected_action=prepare_wave_plan`.
- 2026-04-22: Derived `wave-04/00-wave-plan.yaml` from the remaining `slot_brief_set` entries using the repo’s `mixed_clusters_then_escalate` scoring logic instead of sample artifacts. The resulting scope consumes the last three `sequence_index=2` slots first, then opens four `sequence_index=3` pressure-test slots across `evidence_reading`, `value_conflict`, and `creator_context`. Updated `01-run-state.yaml` to `current_wave_id=wave-04`, `current_role=director`, `current_wave_status=planned`, and the new pending slot scope.
- 2026-04-22: Wrote `wave-04/01-director-note.md` to turn the current wave into a `late_callback_without_resolution` batch: this pass pressure-tests whether earlier interpretive splits start to re-shape clue closure, collaboration boundaries, scheduling parses, callback jokes, testimony framing, and image gaps without allowing any of them to become final answers. The note also pre-splits the two media-required slots into distinct `clue constellation` versus `timeline parse` camera families so they cannot collapse into the same board/wall visual grammar. Updated `01-run-state.yaml` from `director` to `runtime_guard` with `current_wave_status=director_done`.
- 2026-04-22: Wrote `wave-04/02-runtime-guardrails.yaml`, translating the current wave’s unresolved boundaries, entity requirements, actor-source policy, and role-hint translations into writer/visual-ready constraints. This pass also locks `plot-twist-club-02` and `limited-program-02` into distinct `clue constellation` versus `timeline parse` media families with explicit forbidden family overlap, so the next visual step cannot collapse both back into generic board surfaces. Updated `01-run-state.yaml` from `runtime_guard` to `writer` with `current_wave_status=runtime_guard_done`.
- 2026-04-22: Wrote `wave-04/03-slot-content-units.yaml` with 7 passed writer units spanning clue-board pressure, collaboration-boundary parsing, schedule-pressure parsing, callback-joke recirculation, rule-vs-context splitting, testimony reframing, and image-gap disappointment. Bound the 2 media-required slots to future visual refs while keeping the remaining 5 slots text-first, and advanced `01-run-state.yaml` from `writer` to `visual` with `current_wave_status=visual_in_progress`.
- 2026-04-22: Generated 2 final raster visuals for `wave-04` media-required slots via the built-in `image_gen` workflow, copied the selected PNGs into `waves/wave-04/assets/`, and wrote `wave-04/04-visual-units.yaml`. The chosen finals preserve the pre-locked family split: `plot-twist-club-02` as a near-closure public clue surface on glass, and `limited-program-02` as a public routing/schedule surface carrying dry programming pressure. Updated `01-run-state.yaml` from `visual` to `wave_checkpoint` with `current_wave_status=checkpoint_pending`.
- 2026-04-22: Wrote `wave-04/05-wave-checkpoint.yaml` and marked the wave as checkpoint-passed with no hard failures and no residual drift signals. Updated `01-run-state.yaml` from `wave_checkpoint` to `idle`, cleared pending slot ids, and set `next_expected_action=assemble_content_package` so the cumulative package can be refreshed before `wave-04` is formally closed.
- 2026-04-22: Refreshed the cumulative Step 3 package from `wave-01` through `wave-04`, regenerated `package/00-content-package.yaml` and `package/01-review-snapshot.yaml`, and then closed `wave-04` in `01-run-state.yaml`. The run now has `completed_wave_count=4`, `remaining_wave_count=2`, `current_wave_status=closed`, and `next_expected_action=prepare_wave_plan`.
- 2026-04-22: Derived `wave-05/00-wave-plan.yaml` from the remaining `slot_brief_set` entries using the repo’s `mixed_clusters_then_escalate` selection logic instead of sample artifacts. The resulting scope stays entirely inside the remaining `sequence_index=3` layer, selects 7 text-first slots across `evidence_reading`, `value_conflict`, and `creator_context`, and leaves `sequence_index=4` untouched because the current layer can still form a full wave. Updated `01-run-state.yaml` to `current_wave_id=wave-05`, `current_role=director`, `current_wave_status=planned`, and the new pending slot scope.
- 2026-04-22: Wrote `wave-05/01-director-note.md` to make the next wave an `aftertaste_without_prediction` batch: this pass converts the pressure from waves 1-4 into evidence boundaries, blame splits, headline clashes, deep-night aftertaste, next-question carryover, creator watchlists, and collaboration callback threads without letting any of them turn into tomorrow spoilers. Updated `01-run-state.yaml` from `director` to `runtime_guard` with `current_wave_status=director_done`.
- 2026-04-22: Wrote `wave-05/02-runtime-guardrails.yaml`, translating the all-text director intent into machine-readable constraints for 7 text-first slots. This pass keeps all `media_required` flags false, hardens `no prediction / no premature closure / no filler visual` discipline, and preserves each community’s existing role-hint and actor-boundary patterns while advancing `01-run-state.yaml` from `runtime_guard` to `writer` with `current_wave_status=runtime_guard_done`.
- 2026-04-22: Wrote `wave-05/03-slot-content-units.yaml` with 7 passed writer units spanning evidence boundary, layered blame split, deep-night aftertaste, headline framing clash, next-question carryover, creator watchlist, and collaboration callback thread. Kept all units fully text-first with `visual_required=false` and no visual refs, then advanced `01-run-state.yaml` from `writer` to `visual` with `current_wave_status=visual_in_progress` so the empty legal visual batch can be emitted next.
- 2026-04-22: Wrote `wave-05/04-visual-units.yaml` as the empty legal visual batch for an all-text wave: `visual_units: []`, no attachments, no placeholder cards, and no filler visuals. Advanced `01-run-state.yaml` from `visual` to `wave_checkpoint` with `current_wave_status=checkpoint_pending` so `wave-05` can be evaluated without semantic drift from fake media output.
- 2026-04-22: Wrote `wave-05/05-wave-checkpoint.yaml` and marked the wave as checkpoint-passed with no hard failures and no residual drift signals. The checkpoint explicitly treats the empty legal visual batch as the correct output for an all-text wave rather than as a missing artifact. Updated `01-run-state.yaml` from `wave_checkpoint` to `idle`, cleared pending slot ids, and set `next_expected_action=assemble_content_package` so the cumulative package can be refreshed before `wave-05` is formally closed.
- 2026-04-22: Refreshed the cumulative Step 3 package from `wave-01` through `wave-05`, regenerated `package/00-content-package.yaml` and `package/01-review-snapshot.yaml`, and then closed `wave-05` in `01-run-state.yaml`. The run now has `completed_wave_count=5`, `remaining_wave_count=1`, `current_wave_status=closed`, `next_expected_action=prepare_wave_plan`, and a cumulative package of 35 slot content units plus 17 final live visual units.
- 2026-04-22: Derived `wave-06/00-wave-plan.yaml` from the exact final remaining seed pool after the refreshed 5-wave cumulative package rather than reshaping the leftovers by hand. The selected scope consumes all 7 remaining slots in execution order: one residual `sequence_index=3` programming-watch slot (`limited-program-03`) followed by six `sequence_index=4` spillover/callback/follow-list slots (`hot-arena-04`, `emotion-jury-04`, `persona-chaos-04`, `values-stage-04`, `fail-postmortem-04`, `weekly-headline-04`). All remaining slots are text-first, so `media_required_slot_ids=[]`; `01-run-state.yaml` has advanced to `current_wave_id=wave-06`, `current_role=director`, and `current_wave_status=planned`.
- 2026-04-22: Wrote `wave-06/01-director-note.md` to keep the final remaining-pool wave from collapsing into a run-level epilogue. This note reframes the 7 remaining slots as queue watch, late-night public continuation, relationship aftershock, long-form persona reread, rule spillover, mechanism lesson extract, and unresolved follow-list work, all under `final_spillover_without_closure`. It also hardens the all-text discipline again: no `media_required` slots, no placeholder visuals, and no fake closing statement. Updated `01-run-state.yaml` from `director` to `runtime_guard` with `current_wave_status=director_done`.
- 2026-04-22: Wrote `wave-06/02-runtime-guardrails.yaml`, translating the final-wave director intent into machine-readable constraints for 7 text-first slots. This pass locks every remaining slot to its unresolved boundary, preserves the seed-bound entity and actor policies, keeps `limited-program-03` as a queue-watch/programming lens rather than an internal schedule leak, and hardens the wave-level rules that the final batch is still not a final answer: no future-fact drift, no fake closure, and no filler visual backfill. Updated `01-run-state.yaml` from `runtime_guard` to `writer` with `current_wave_status=runtime_guard_done`.
- 2026-04-22: Wrote `wave-06/03-slot-content-units.yaml` with 7 passed writer units aligned to the exact final remaining pool. This pass keeps the whole wave fully text-first while still giving each community a distinct endgame function: `limited-program-03` as queue watch, `hot-arena-04` as late-night continuation, `emotion-jury-04` as relationship aftershock, `persona-chaos-04` as long-form style reread, `values-stage-04` as rule spillover, `fail-postmortem-04` as mechanism lesson extract, and `weekly-headline-04` as unresolved follow-list. Updated `01-run-state.yaml` from `writer` to `visual` with `current_wave_status=visual_in_progress` so the empty legal visual batch can be emitted next.
- 2026-04-22: Wrote `wave-06/04-visual-units.yaml` as the empty legal visual batch for the final all-text wave: `visual_units: []`, no attachments, no placeholder cards, and no filler visuals. Updated `01-run-state.yaml` from `visual` to `wave_checkpoint` with `current_wave_status=checkpoint_pending` so `wave-06` can be evaluated without semantic drift from fake media output.
- 2026-04-22: Wrote `wave-06/05-wave-checkpoint.yaml` and marked the final wave as checkpoint-passed with no hard failures and no residual drift signals. The checkpoint explicitly accepts `visual_units: []` as the correct formal output for this no-media final wave, and confirms that the last remaining pool closed as spillover/watchlist work rather than as a run-level ending.
- 2026-04-22: Refreshed the cumulative Step 3 package from `wave-01` through `wave-06`, regenerated `package/00-content-package.yaml` and `package/01-review-snapshot.yaml`, and closed the live run into the Step 3 terminal handoff state. The run now has `completed_wave_count=6`, `remaining_wave_count=0`, `run_meta.status=ready_for_editorial_review`, `current_wave_status=closed`, `next_expected_action=handoff_to_editorial_review`, and a final cumulative package of 42 slot content units plus 17 final live visual units.
- 2026-04-22: Prepared the Step 4 kickoff-exec review workspace by running `prepare-kickoff-exec-review.ts` against the final Step 3 run. This created kickoff-exec run `2026-04-22T07-25-33-474Z-e9ae47e0` with linked `review/00-editorial-review.yaml`, `freeze/00-freeze-manifest.yaml`, `freeze/01-export-overrides.yaml`, and import-history scaffolding for the approved Step 3 package.
- 2026-04-22: Completed the Step 4 editorial review in `review/00-editorial-review.yaml`. The package received `verdict.disposition=approve` and `review_status=approved` because the 42-slot/17-visual package holds community coverage, staged wave progression, unresolved discipline, and correct all-text-wave behavior; `export_readiness.ready_to_freeze` intentionally remains `false` because operator export-layer fields are still missing and were not mutated during Step 4.
- 2026-04-22: Entered Step 5 and completed the operator export layer in `freeze/01-export-overrides.yaml`. Filled bundle-level fields with `bundle_id=kickoff-foundation-20260422-01` and `baseline_label=Kickoff Foundation 2026-04-22 01`, backfilled the 4 unresolved `programming_daypart` slots, then batch-derived `scheduled_local_time`, `phase`, and `editorial_shelf_id` for all 42 export specs from package order, slot suffix progression, and existing launch-community shelf semantics without touching Step 3 title/body/tags.
- 2026-04-22: Ran `export-kickoff-exec-bundle.ts` for kickoff-exec run `2026-04-22T07-25-33-474Z-e9ae47e0`. The service exported the local kickoff bundle to `/.ai/.tmp/kickoff/`, copied 17 media assets, generated `manifest.v1.yaml`, validated it via active `loadKickoffBundle()`, rewrote `freeze/00-freeze-manifest.yaml` and `freeze/01-export-overrides.yaml` to `exported`, updated `review/00-editorial-review.yaml.export_readiness` to ready, refreshed logical entity `export_spec_id` bindings, and moved kickoff-exec `current-run.yaml` / `01-run-state.yaml` into `kickoff-bundle-exported` / `freeze_exported`.
- 2026-04-22: Added `render-kickoff-bundle-preview.ts` under kickoff-local dev tooling and used it to materialize `/.ai/.tmp/kickoff/preview.html` from the exported kickoff manifest. The preview groups all 42 posts by programming daypart, renders the copied media assets inline, and exposes title/body/storyline/shelf/phase metadata so the exported bundle can be reviewed as a human-readable page instead of raw YAML.
- 2026-04-22: Attempted a direct local `launch.kickoff` import against the exported bundle and found the local dev database was missing the launch system roster agents required by `bootstrapLaunchRosterMemberships()`. Instead of forcing the import path, ran `pnpm seed -- --profile=launch --skip-bio` to materialize the 12 launch communities plus 40 launch-system agents in the local dev database with the correct launch identity config and memberships.
- 2026-04-22: Re-ran `pnpm launch.kickoff --manifest-path=.ai/.tmp/kickoff/manifest.v1.yaml` after the launch seed. The import then succeeded, creating active kickoff baseline `cmo9rfsvl00069jnowkcc0bum`, kickoff batch `cmo9rfsvr00079jnohrg0cfzw`, and 42 imported kickoff posts mapped onto the seeded launch communities/system roster agents in the local dev database.
- 2026-04-22: Removed the temporary exported-bundle static preview page `/.ai/.tmp/kickoff/preview.html` after the user chose the real local DB import path instead of file-based preview review.
- 2026-04-22: Tightened the local dev data-mode boundary so kickoff and legacy mock data cannot coexist in the public dataset. Added `resetNonGovernedDevSeedPublicFixtures()` to [dev-seed-runner.ts](/Volumes/DataDisk/Project/Fun-ForumAI/src/backend/dev/dev-seed-runner.ts), wired `runDevSeed(profile=launch)` to clear canonical/smoke-minimal public fixtures before rebuilding the launch base, and wired [launch-kickoff.ts](/Volumes/DataDisk/Project/Fun-ForumAI/src/backend/dev/launch-kickoff.ts) to run the same cleanup before importing a kickoff bundle.
- 2026-04-22: Expanded the exclusive-mode cleanup to remove dependent audience artifacts for legacy mock posts (`audience_threads`, `audience_messages`, `audience_summaries`, and `aftershow_callouts`) before post deletion. This resolved the `audience_threads_post_id_fkey` failure that initially blocked `pnpm seed -- --profile=launch --skip-bio`.
- 2026-04-22: Re-seeded the local dev database into a clean launch-only base after the exclusivity patch, then re-imported the kickoff bundle. The new local active kickoff baseline is `cmo9sf2sx0006fbno6vwv030p`; the database now holds only `generation_mode='kickoff_import'` public posts, with no remaining mock/canonical public posts.
- 2026-04-22: Downgraded `smoke-minimal` from a public interactive dev mode to an internal automation-only fixture path. Removed the `加载 Smoke` button from [DevAuthToolbar.tsx](/Volumes/DataDisk/Project/Fun-ForumAI/src/frontend/widgets/dev/DevAuthToolbar.tsx), narrowed the frontend-exposed dev seed profile type in [dev.ts](/Volumes/DataDisk/Project/Fun-ForumAI/src/frontend/api/hooks/dev.ts) to public profiles only, and documented in [dev-seed.ts](/Volumes/DataDisk/Project/Fun-ForumAI/src/backend/routes/dev-seed.ts) that `smoke-minimal` remains supported only for internal automation such as mobile smoke preparation.
- 2026-04-22: Tightened exclusivity one step further so canonical/mock loads also evict residual `smoke-minimal` fixtures. Generalized `resetNonGovernedDevSeedPublicFixtures()` in [dev-seed-runner.ts](/Volumes/DataDisk/Project/Fun-ForumAI/src/backend/dev/dev-seed-runner.ts) to accept profile targets, then wired `runDevSeed(profile=canonical)` to clear `smoke-minimal` before seeding canonical fixtures while keeping `launch/kickoff` on the broader `canonical + smoke-minimal` cleanup path.
