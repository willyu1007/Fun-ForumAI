# 03 Implementation Notes

- 2026-03-16: Task created to independently verify the residual-risk review against the current owner experience before making further changes.
- 2026-03-16: Residual-risk triage result:
  - `R2` / `R3` / part of `R4` / part of `R5` were true in the shipped owner path.
  - `R1` / `R6` / `R7` / `R9` / `R10` remain valid product or operational recommendations, but not direct code defects that could be closed in this pass.
- 2026-03-16: Owner profile header was de-control-planed without removing access:
  - owner-facing tab label switched from `成就线` to `编年史` while preserving `tab=achievements`
  - header CTA switched from plain `私聊` to `带一段经历给她`
  - `身份契约` label changed to `角色底色`
  - `声誉 / 人格 v / owner_id / model / id` no longer sit in the hero; management metadata is now behind an owner/admin-only toggle
- 2026-03-16: Owner life-home fallback copy was rewritten away from raw system wording:
  - `degraded` badge became `轻读模式`
  - overview fallbacks now read as a forming life chapter instead of a missing-data panel
  - owner chronicle intro now says `故事接点` instead of `story beat`
- 2026-03-16: Live browser verification exposed an additional real drift in the read model:
  - some owner beats still surfaced `系统捕捉到...` summaries and legacy `owner 线` scene labels from persisted `story_meta_v1`
  - owner suggestion lane still used tool/control phrasing such as `私聊闭环` and `开始私聊`
- 2026-03-16: Backend owner read models were updated to humanize these remaining drifts at read time:
  - signal fallback summaries now read as owner narrative residue, not system capture
  - legacy persisted `owner 线` scene labels are normalized to `私域余温` during `readChronicleStoryMeta`
  - owner suggestion lane now uses `只属于你们的经历 / 余温` wording and CTA labels aligned with the new header CTA
