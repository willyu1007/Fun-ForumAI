# 03 Implementation Notes

- 2026-03-15: 新建 T-103 task bundle，映射到 `F-020 > R-031`，准备开始 schema/runtime/UI 三线实施。
- 2026-03-15: 新增 `AgentInferenceProfile` Prisma 模型与 repository 读写接口，持久化 incumbent/challenger/migration/freeze/lastSnapshot。
- 2026-03-15: 实现 `InferenceProfileService`，采用“PersonaState 主导 + Stats 有限偏置”的两段式编译，输出 `TemperamentAxes`、`InferenceSignals`、family scores、narrative summary。
- 2026-03-15: 将 visible runtime 路径统一接入 `InferenceProfileService.resolveVisibleRoute()`，替换多处硬编码 `base/lite` 的 tier 请求。
- 2026-03-15: 控制面补充 `/agents/:agentId/inference-profile` admin 动作接口，支持 `approve_shadow`、`block_challenger`、`set_manual_lock`。
- 2026-03-15: 新增 `AgentInferenceShadowReview` 持久化与 in-memory/pg repository 接口，记录 evidence window、compare summary、review case 和审批状态。
- 2026-03-15: `InferenceProfileService` 补齐 shadow compare 生命周期：`startShadowReview()` 冻结 evidence window，`collectShadowReview()` 汇总 ledger/observability 证据，`approveShadow()` 只在 collected+approve 时触发 rare reanchor。
- 2026-03-15: owner/profile/stats 只暴露 narrative，admin/profile 额外暴露 raw compile snapshot；新增 runtime compiler metrics 卡片。
- 2026-03-15: admin profile debug 卡片补充 shadow compare 诊断和控制按钮，支持 `start / collect / approve / block / manual lock`。
- 2026-03-15: 重写外部策略文档 `/Users/yurui/Downloads/agent-personality-model-strategy.md`，统一为“身份链优先、编译层治理化”语义。
- 2026-03-15: 新增 `.ai/llm-config/registry/provider_admission.yaml`，按 visible `voice_line_id` 建 admitted/shadow/blocked provider 池，并为腾讯/Ark 等未来槽位保留 shadow/blocked 准入位。
- 2026-03-15: `registry-loader` 与 `validate-llm-registry.mjs` 增加 provider admission 合约校验，强制每个 visible voice line 至少保留一个 admitted 候选。
- 2026-03-15: `llm-gateway` visible 路由改为 admitted-only 过滤，shadow/blocked 候选不会进入 active route plan；admin runtime features 新增 provider admission 总览卡片。
- 2026-03-15: `XpService` 增加 non-Prisma in-memory fallback，确保 growth gate、shadow compare 和 agent XP 接口在 e2e / 本地内存容器中保持可用。
- 2026-03-15: code review 修复 4 个一致性问题：
  - `collect_shadow_review` 不再隐式创建 review，避免 evidence window 起点落在 ledger 之后；
  - `approve_shadow` 增加 `canRareReanchor` growth gate，防止 5 点 growth 直接 rare reanchor；
  - `setManualVoiceLineLock(false)` 会重新计算 blockedReason/migrationState，解除 manual lock 后不再卡在 `blocked/manual_lock`；
  - inference-profile 非法状态流转改为 `ValidationError`，控制面返回 400 而不是 500。
- 2026-03-15: admin profile 页的 shadow compare 按钮约束与后端状态机对齐，`collect` 仅在 `running` review 时可用。
- 2026-03-15: 收口 audit 中发现的 read-surface 回归：`GET /v1/agents/:agentId/profile` 的 admin `inference_profile_debug` 之前漏掉 `shadowReview`，导致前端在 `start_shadow_review` 后无法通过 profile refetch 拿到 running/collected 状态，`collect/approve` 按钮会错误禁用。现已补齐返回字段，并新增 e2e 覆盖。
- 2026-03-15: 当前 provider admission 仍主要是“静态 registry + gateway guardrail + admin summary”模式；后续需要继续补 provider 级 compare automation、evidence job 和 rollout playbook。
