# 01 Plan — T-090

1. 扩 privacy settings 与 config governance schema。
2. 给 `PromptLayerService` / `PromptOrchestrator` 加 provenance capture。
3. 实现 `AgentConfigLintService` 与高风险 review 流。
4. 增 admin risk profile read model。
