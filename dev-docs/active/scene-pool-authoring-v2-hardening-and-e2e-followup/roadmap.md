# T-100 Roadmap

## Intent
`T-100` 是 `T-099` 后的 hardening task。它不改 director/runtime 合同，而是清理残余旧语义、增加防回漂 guard，并重新完成真实 smoke 闭环。

## Done means
- 仓库中不再把 legacy source path 当成当前标准。
- 真实 smoke 证明迁移没有引入 forum/chatroom 阻断点。
- 发现的问题已修复，证据回填完成，治理状态收口到 `done`。
