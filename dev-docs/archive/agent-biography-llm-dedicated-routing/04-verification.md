# 04 Verification — T-206

- 2026-04-21 | `nl -ba src/backend/services/biography-writer-service.ts | sed -n '280,430p'` | pass | Confirmed biography chapter render and later-note render are hard-bound to `qwen-director-v1` and the shared `hidden-public_observation_digest-agent-bio-base` execution policy.
- 2026-04-21 | `nl -ba src/backend/llm/llm-gateway.ts | sed -n '621,640p'` | pass | Confirmed initial profile resolution is driven by `homeVoiceLineId + intent + requestedTier`, so policy-only changes cannot isolate biography model routing.
- 2026-04-21 | `nl -ba .ai/llm-config/registry/model_profiles.yaml | sed -n '618,646p'` plus `sed -n '1264,1278p'` | pass | Confirmed the current biography path resolves through the shared `qwen-director-public-observation-base`, while `kimi-deep-public-observation-base` exists but is not biography-exclusive.
- 2026-04-21 | `nl -ba .ai/llm-config/registry/execution_policies.yaml | sed -n '213,228p'` plus `nl -ba src/backend/services/agent-bio-render-service.ts | sed -n '597,623p'` | pass | Confirmed the current `hidden-public_observation_digest-agent-bio-base` policy is also used by agent-social-bio render, so it cannot be repurposed as a biography-only policy without cross-impact.
- 2026-04-21 | `nl -ba src/shared/agent-persona-catalog.ts | sed -n '11,20p'` and `sed -n '234,277p'` | pass | Confirmed any new biography routing line must be added to the voice-line catalog as a hidden-only entry to remain type-safe and governance-clean.
- 2026-04-21 | User alignment on dedicated biography route reusing the same `LLMGateway` injection surface plus `node .ai/scripts/ctl-project-governance.mjs lint --check --project main` | pass | Recorded the decision that `T-206` will isolate routing/profile/policy only and will not introduce a second biography-specific API client or provider wrapper.
- 2026-04-21 | User alignment on split routing posture plus `node .ai/scripts/ctl-project-governance.mjs lint --check --project main` | pass | Recorded the first-rollout decision to route chapter render through a Kimi-first biography premium lane and later-note render through a Qwen-first biography base lane.
- 2026-04-21 | User alignment on routing-only first rollout plus `node .ai/scripts/ctl-project-governance.mjs lint --check --project main` | pass | Recorded the decision that the first `T-206` implementation slice will not modify biography prompt text or prompt versions; only routing/profile/policy bindings may change.
- 2026-04-21 | User alignment on direct `Kimi primary` chapter rollout plus `node .ai/scripts/ctl-project-governance.mjs lint --check --project main` | pass | Recorded the decision that chapter render may go directly to `Kimi primary` in the first rollout as long as env-gated real-LLM smoke coverage and rollback conditions are implemented in the same slice.
- 2026-04-21 | `pnpm llm:routing:generate` | pass | Regenerated `src/backend/llm/generated/voice-line-routing.generated.ts` after adding `biography-director-v1` plus biography-only base/premium profiles and policies.
- 2026-04-21 | `pnpm exec vitest run src/backend/llm/__tests__/registry-contract.test.ts src/backend/llm/__tests__/callsite-inventory.test.ts` | pass | Verified the new biography voice line and callsite inventory are registry-consistent and do not rely on provider-admission pools.
- 2026-04-21 | `pnpm exec vitest run src/backend/llm/__tests__/registry-contract.test.ts src/backend/llm/__tests__/llm-gateway.test.ts src/backend/llm/__tests__/callsite-inventory.test.ts src/backend/services/__tests__/biography-writer-service.test.ts src/backend/services/__tests__/agent-biography-service.test.ts src/backend/services/__tests__/agent-biography-real-smoke.test.ts` | pass | Verified biography routing resolution (`Kimi premium`, `Qwen base`), same-line fallback, writer callsite bindings, unchanged chapter compile behavior, and the env-gated smoke harness registration. The live-provider smoke tests were skipped because required provider keys were absent.
- 2026-04-21 | `pnpm exec tsc -p tsconfig.json --noEmit` | pass | Confirmed the new voice-line id, registry bindings, writer rebinding, and smoke harness all typecheck cleanly.
- 2026-04-21 | `zsh -lc 'if [[ -n "$MOONSHOT_API_KEY" ]]; then echo MOONSHOT_API_KEY=present; else echo MOONSHOT_API_KEY=missing; fi; if [[ -n "$DASHSCOPE_API_KEY" ]]; then echo DASHSCOPE_API_KEY=present; else echo DASHSCOPE_API_KEY=missing; fi'` | pass | Confirmed this shell currently lacks both live-provider credentials, explaining why the env-gated smoke suite was skipped rather than executed online.
- 2026-04-21 | `RUN_REAL_LLM_BIOGRAPHY_SMOKE=1 pnpm exec vitest run src/backend/services/__tests__/agent-biography-real-smoke.test.ts` | pass | Confirmed the real-smoke gate itself behaves correctly: the suite remains skipped when the explicit opt-in flag is present but required provider credentials are still missing.
- 2026-04-21 | `pnpm prisma format && pnpm prisma generate` | pass | Confirmed the expanded biography writer telemetry schema and migration remain compatible with the generated Prisma client after adding repair/rescue/audit fields.
- 2026-04-21 | `node .ai/skills/workflows/llm/llm-engineering/scripts/validate-llm-registry.mjs` | pass | Confirmed the biography-only registry additions for `moonshot-v1-128k`, `kimi-k2.5`, updated pricing, capabilities, policies, and prompt refs remain registry-valid.
- 2026-04-21 | `pnpm exec vitest run src/backend/llm/__tests__/registry-contract.test.ts src/backend/llm/__tests__/llm-gateway.test.ts src/backend/services/__tests__/biography-writer-service.test.ts src/backend/services/__tests__/biography-factual-audit-service.test.ts src/backend/services/__tests__/agent-biography-service.test.ts src/backend/services/__tests__/agent-biography-real-smoke.test.ts` | pass | Verified final `T-206` landing behavior: biography premium resolves to `moonshot-v1-128k`, same-line fallback remains available, later-note stays on `qwen3.5-plus`, deterministic repair and rescue render paths are covered, and the env-gated smoke suite remains registered in the target run.
- 2026-04-21 | `pnpm exec tsc -p tsconfig.json --noEmit` | pass | Reconfirmed the final landing typechecks cleanly after prompt v2, repair telemetry, rescue-render plumbing, and persistence updates.
- 2026-04-21 | `node .ai/scripts/ctl-project-governance.mjs sync --apply --project main && node .ai/scripts/ctl-project-governance.mjs lint --check --project main` | pass | Synced the `T-206` task bundle from `in-progress` to `done`, regenerated the project hub views, and revalidated the registry/task index after the final landing notes were updated.
- 2026-04-21 | `env MOONSHOT_API_KEY=*** DASHSCOPE_API_KEY=*** RUN_REAL_LLM_BIOGRAPHY_SMOKE=1 pnpm exec vitest run src/backend/services/__tests__/agent-biography-real-smoke.test.ts` | pass | Executed the real-provider smoke suite end to end with temporary keys. Observed `moonshot-v1-128k` serving the biography premium route online, `kimi-k2.5` serving the pinned premium rescue path, and the biography base route successfully falling back from `qwen3.5-plus` timeout to `kimi-k2.5` for later-note publish.
- 2026-04-21 | `pnpm prisma migrate deploy` | pass | Real E2E seeding exposed a local environment drift: `20260421153000_t206_biography_writer_hardening` had not been applied, so writer telemetry inserts failed on missing `repair_applied` columns. Applying repo migrations fixed the persistence contract and unblocked end-to-end verification.
- 2026-04-21 | historical command `pnpm run seed:agent-biography:review` (removed on 2026-04-21 after the showcase was merged into canonical `pnpm seed`) | pass | Rebuilt the canonical review fixtures after the migration fix. Confirmed the published sample (`642c20df-e181-432b-8862-67d3e830a6c8`) serves a non-degraded four-chapter biography with one inline later note, while degraded and empty-placeholder samples still seed correctly.
- 2026-04-21 | `pnpm exec vitest run src/backend/routes/__tests__/e2e-read-api.test.ts -t "biography"` | pass | Revalidated the read surface after the persistence fix: `GET /v1/agents/:agentId/biography-book` returns the shared book contract and `POST /v1/agents/:agentId/biography-book/telemetry` still accepts reading events on the live route.
- 2026-04-21 | `curl -sS http://localhost:3000/v1/agents/642c20df-e181-432b-8862-67d3e830a6c8/biography-book | jq '.data.chapters'` plus `curl -sS 'http://localhost:3000/v1/agents/642c20df-e181-432b-8862-67d3e830a6c8/biography-book?chapter_id=cmo83ly81000qrpno3oamka5j'` | pass | Confirmed the published fixture exposes the full chapter directory through the shared endpoint and that a chapter-specific request renders the revised “后来补记” chapter with inline `later_notes`, not a pseudo chapter.
- 2026-04-21 | `curl -sS -o /tmp/t206_bio_404.json -w '%{http_code}' http://localhost:3000/v1/agents/not-a-real-agent/biography-book` plus invalid telemetry `curl -sS -o /tmp/t206_bio_telemetry_400.json -w '%{http_code}' -X POST .../biography-book/telemetry --data '{\"event_type\":\"not_valid\"}'` | pass | Negative-path probes returned the expected `404 NOT_FOUND` and `400 VALIDATION_ERROR` envelopes, confirming the new biography route rejects invalid agent IDs and event types instead of silently swallowing bad inputs.
- 2026-04-21 | `curl -sS -X POST http://localhost:3000/v1/agents/642c20df-e181-432b-8862-67d3e830a6c8/biography-book/telemetry ... 'history_directory_opened'` plus `pnpm exec tsx --eval \"import { getPrismaClient, disconnectPrisma } ...\"` | pass | Verified side effects against the live Postgres backing the dev server: the probe telemetry event was persisted in `agentBiographyReadTelemetryEvent`, proving the UI/API route and the inspected database are the same chain.
- 2026-04-21 | Browser check on `http://localhost:3000/agents/642c20df-e181-432b-8862-67d3e830a6c8/history` plus screenshot capture to `.ai/.tmp/biography-t206-e2e-published.png` | pass | Confirmed the real readonly page renders the single-book reading experience end to end, issues `GET /v1/agents/:agentId/biography-book` and `POST /v1/agents/:agentId/biography-book/telemetry`, and does so without console errors.
- 2026-04-21 | Real-provider smoke review plus `.ai/llm-config/registry/model_profiles.yaml` cleanup, `pnpm llm:routing:generate`, and `node .ai/skills/workflows/llm/llm-engineering/scripts/validate-llm-registry.mjs` | pass | Online smoke surfaced a route drift bug: biography base still had `token-plan-openai/qwen3.6-plus` ahead of the intended DashScope candidate. Removing that candidate from the biography-only base profile restored the agreed `qwen3.5-plus -> kimi-k2.5` order without touching other Qwen lines.
- 2026-04-21 | `pnpm exec vitest run src/backend/llm/__tests__/registry-contract.test.ts src/backend/llm/__tests__/llm-gateway.test.ts src/backend/services/__tests__/biography-writer-service.test.ts` plus `env MOONSHOT_API_KEY=*** DASHSCOPE_API_KEY=*** RUN_REAL_LLM_BIOGRAPHY_SMOKE=1 pnpm exec vitest run src/backend/services/__tests__/agent-biography-real-smoke.test.ts` | pass | Post-fix regression confirmed the biography base tests now assert `dashscope-openai/qwen3.5-plus`, and the live-provider smoke proved the final runtime order: later-note first attempts DashScope Qwen, then cleanly falls back to `kimi-k2.5` on provider timeout while chapter publish continues to use `moonshot-v1-128k` with `kimi-k2.5` rescue available.
