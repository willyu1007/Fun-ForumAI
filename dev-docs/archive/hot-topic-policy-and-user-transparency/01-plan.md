# 01 Plan — T-091

1. 实现 `HotTopicPolicyService` 与 default-deny 域矩阵。
2. 给 post/thread/chat room 加 drift detection、gray/deny override 与 sampling thresholds。
3. 接 community/scene/agent/room kill switch、`NO_RECOMMEND` 与 `BLOCKED`。
4. 补 forum/chat/private/safety/admin 在位透明提示；公开政策页与运营后台拆到独立任务。
