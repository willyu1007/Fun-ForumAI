# 01 Plan

1. 汇总 `T-117` ~ `T-124` 任务包、提交记录、关键代码入口，建立状态矩阵。 `[in-progress]`
2. 跑媒体相关测试与全仓 `typecheck`，定位确定性回归。 `[in-progress]`
3. 修复类型漂移与主链缺陷，补充必要测试。 `[pending]`
4. 结合 requirements doc 评估 coverage / drift / omission。 `[pending]`
5. 运行验证并输出结论与 follow-up 建议。 `[pending]`
