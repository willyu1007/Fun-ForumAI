# Generated file. DO NOT EDIT BY HAND.
# Source of truth: env/contract.yaml (repo-env-contract)
# Generated at: 2026-03-10T10:04:58Z
#
# Usage:
#   - Copy to .env.local (gitignored) and fill values.
#   - Do NOT commit .env.local.

# APP_ENV: Deployment environment profile.; type=enum; required; default='dev'
APP_ENV=dev

# CONTROL_PLANE_SCHEDULER_BACKOFF_BASE_MS: Base exponential backoff delay in milliseconds for scheduler retries.; type=int; optional; default=30000
CONTROL_PLANE_SCHEDULER_BACKOFF_BASE_MS=30000

# CONTROL_PLANE_SCHEDULER_BACKOFF_MAX_MS: Max retry backoff delay in milliseconds for scheduler retries.; type=int; optional; default=900000
CONTROL_PLANE_SCHEDULER_BACKOFF_MAX_MS=900000

# CONTROL_PLANE_SCHEDULER_BATCH_LIMIT: Max scheduled config patches processed per scheduler tick.; type=int; optional; default=20
CONTROL_PLANE_SCHEDULER_BATCH_LIMIT=20

# CONTROL_PLANE_SCHEDULER_INTERVAL_MS: Poll interval in milliseconds for community config scheduled activation.; type=int; optional; default=30000
CONTROL_PLANE_SCHEDULER_INTERVAL_MS=30000

# CONTROL_PLANE_SCHEDULER_MAX_RETRIES: Max retry attempts for failed scheduled config applications.; type=int; optional; default=5
CONTROL_PLANE_SCHEDULER_MAX_RETRIES=5

# CONTROL_PLANE_SCHEDULER_STARTUP_DELAY_MS: Startup delay in milliseconds before the first community config scheduler scan.; type=int; optional; default=5000
CONTROL_PLANE_SCHEDULER_STARTUP_DELAY_MS=5000

# CORS_ORIGINS: Comma-separated list of allowed CORS origins.; type=string; optional; default='http://localhost:3000'
CORS_ORIGINS=http://localhost:3000

# DASHSCOPE_API_KEY: DashScope / Qwen provider API key.; type=string; optional; secret_ref=dashscope_api_key
DASHSCOPE_API_KEY=<secret:dashscope_api_key>

# DATABASE_URL: PostgreSQL connection URL (Prisma datasource).; type=string; required; secret_ref=database_url
DATABASE_URL=<secret:database_url>

# DEEPSEEK_API_KEY: DeepSeek provider API key.; type=string; optional; secret_ref=deepseek_api_key
DEEPSEEK_API_KEY=<secret:deepseek_api_key>

# EXPO_EAS_PROJECT_ID: Expo EAS project id used for development builds and EAS metadata injection in the mobile app config.; type=string; optional
EXPO_EAS_PROJECT_ID=

# EXPO_PUBLIC_API_BASE_URL: Mobile API base URL override. If unset, iOS simulator defaults to http://127.0.0.1:4000 and Android emulator defaults to http://10.0.2.2:4000.; type=string; optional
EXPO_PUBLIC_API_BASE_URL=

# FF_ACHIEVEMENT_CHRONICLE_V1: Enable achievement + chronicle write pipeline and owner/admin read surfaces.; type=enum; optional; default='false'
FF_ACHIEVEMENT_CHRONICLE_V1=false

# FF_ACHIEVEMENT_PUBLIC_HIGHLIGHTS: Enable public highlights endpoint and feed/profile badge/tagline exposure.; type=enum; optional; default='false'
FF_ACHIEVEMENT_PUBLIC_HIGHLIGHTS=false

# FF_AFTERSHOW_AUDIENCE_SUMMARY_V1: Enable audience summary bridge for aftershow triggers.; type=enum; optional; default='false'
FF_AFTERSHOW_AUDIENCE_SUMMARY_V1=false

# FF_AFTERSHOW_EVENT_PIPELINE_V1: Enable due/snapshot/compose/publish aftershow event pipeline.; type=enum; optional; default='false'
FF_AFTERSHOW_EVENT_PIPELINE_V1=false

# FF_AFTERSHOW_V1: Enable aftershow trigger API with OFF/THRESHOLD/PERIODIC/MANUAL modes.; type=enum; optional; default='false'
FF_AFTERSHOW_V1=false

# FF_AGENT_STATS_BEHAVIOR: Enable stats-derived behavior bias wiring in allocator/chat/memory paths.; type=enum; optional; default='false'
FF_AGENT_STATS_BEHAVIOR=false

# FF_AGENT_STATS_RELATION_POLICY: Enable stats-aware relation policy multipliers and thresholds.; type=enum; optional; default='false'
FF_AGENT_STATS_RELATION_POLICY=false

# FF_AGENT_STATS_UI: Enable stats UI exposure for owner panel and related explainers.; type=enum; optional; default='false'
FF_AGENT_STATS_UI=false

# FF_AGENT_STATS_V1: Enable Agent Stats v1 data model and owner APIs.; type=enum; optional; default='false'
FF_AGENT_STATS_V1=false

# FF_AGENT_STATS_VOTE_POLICY: Enable stats-aware vote policy derivation and vote->relation signal wiring.; type=enum; optional; default='false'
FF_AGENT_STATS_VOTE_POLICY=false

# FF_ALLOCATOR_PPR_ENABLED: Enable allocator GraphRelevanceProvider (offline PPR snapshot) scoring term.; type=enum; optional; default='false'
FF_ALLOCATOR_PPR_ENABLED=false

# FF_AUDIENCE_AFTERSHOW_WEB_V1: Enable audience + aftershow web presentation fields on post detail.; type=enum; optional; default='false'
FF_AUDIENCE_AFTERSHOW_WEB_V1=false

# FF_AUDIENCE_ZONE_V1: Enable audience thread/message APIs and persistence models.; type=enum; optional; default='false'
FF_AUDIENCE_ZONE_V1=false

# FF_CASTING_DIRECTOR_ENABLED: Enable casting director role allocation (core/contrast/wildcard) on allocator output.; type=enum; optional; default='false'
FF_CASTING_DIRECTOR_ENABLED=false

# FF_CASTING_DIRECTOR_V2: Enable director v2 hard guards (thread dominance cap + cooldown) and stricter pooling.; type=enum; optional; default='true'
FF_CASTING_DIRECTOR_V2=true

# FF_CHRONICLE_METRICS_CACHE_V1: Enable metrics cache + repository-level aggregation path for achievements metric collection.; type=enum; optional; default='false'
FF_CHRONICLE_METRICS_CACHE_V1=false

# FF_CHRONICLE_SIGNAL_POLICY_V2: Enable stricter chronicle signal visibility policy and public signal quality threshold.; type=enum; optional; default='false'
FF_CHRONICLE_SIGNAL_POLICY_V2=false

# FF_COMMUNITY_DIGEST_V1: Enable community culture digest generation and prompt profile digest injection.; type=enum; optional; default='true'
FF_COMMUNITY_DIGEST_V1=true

# FF_COMMUNITY_PROMPT_PROFILE_V1: Enable structured community prompt profile compilation from community.rules_json.; type=enum; optional; default='false'
FF_COMMUNITY_PROMPT_PROFILE_V1=false

# FF_CONTROL_PLANE_CONFIG_V1: Enable control-plane config proposal/approve/apply/rollback workflow.; type=enum; optional; default='false'
FF_CONTROL_PLANE_CONFIG_V1=false

# FF_EVENT_CONTRACT_V1: Enable event contract and routing enforcement path.; type=enum; optional; default='false'
FF_EVENT_CONTRACT_V1=false

# FF_GLOBAL_HIGHLIGHTS_V1: Enable grouped global highlights API and frontend highlights page entry.; type=enum; optional; default='true'
FF_GLOBAL_HIGHLIGHTS_V1=true

# FF_GUIDANCE_V1: Backend toggle for guidance routes, state transitions, SSE updates, and event ingestion.; type=enum; optional; default='false'
FF_GUIDANCE_V1=false

# FF_HUMAN_PARTICIPATION_V1: Enable human participation routes and human vote/follow capabilities.; type=enum; optional; default='true'
FF_HUMAN_PARTICIPATION_V1=true

# FF_INCUBATION_ORCHESTRATOR_V1: Enable PRIVATE_DIGEST -> incubation seed orchestration.; type=enum; optional; default='false'
FF_INCUBATION_ORCHESTRATOR_V1=false

# FF_INCUBATION_TRUST_HARD_ENFORCE: Enforce structured trust_context checks for strict T4 posts.; type=enum; optional; default='false'
FF_INCUBATION_TRUST_HARD_ENFORCE=false

# FF_INCUBATION_V1: Enable T4 incubation pipeline APIs and persistence models.; type=enum; optional; default='false'
FF_INCUBATION_V1=false

# FF_LAYER_STACK_V2: Enable shared prompt layer composition for runtime and chatroom paths.; type=enum; optional; default='false'
FF_LAYER_STACK_V2=false

# FF_MEMBERSHIPS_V1: Enable explicit agent-community membership management and allocator membership gating.; type=enum; optional; default='true'
FF_MEMBERSHIPS_V1=true

# FF_MEMBERSHIP_STATUS_V1: Enable membership status model (ACTIVE/MUTED/BANNED) and runtime/casting hard blocks.; type=enum; optional; default='false'
FF_MEMBERSHIP_STATUS_V1=false

# FF_MULTIMODAL_AGENT_INCLINATION_V1: Enable multimodal inclination asset pipeline and related APIs.; type=enum; optional; default='false'
FF_MULTIMODAL_AGENT_INCLINATION_V1=false

# FF_NURTURE_PIPELINE_V2: Enable nurture orchestrator realtime/scheduled closure pipeline.; type=enum; optional; default='false'
FF_NURTURE_PIPELINE_V2=false

# FF_PERSONA_RUNTIME_SCENES: CSV whitelist of scenes where persona runtime state should participate.; type=string; optional; default='forum_post,forum_comment,chat_room,private_chat,proactive_dm,scheduled_post'
FF_PERSONA_RUNTIME_SCENES=forum_post,forum_comment,chat_room,private_chat,proactive_dm,scheduled_post

# FF_PERSONA_RUNTIME_V1: Enable persona runtime projection, overlay lifecycle, and render-tier state handling.; type=enum; optional; default='true'
FF_PERSONA_RUNTIME_V1=true

# FF_PERSONA_WRITEBACK_V1: Enable persona runtime writeback after visible render and nurture-derived updates.; type=enum; optional; default='true'
FF_PERSONA_WRITEBACK_V1=true

# FF_PPR_REFRESH_V2: Enable PPR refresh v2 strategy (incremental active sources + daily full backfill).; type=enum; optional; default='true'
FF_PPR_REFRESH_V2=true

# FF_PROMPT_AUDIT_V1: Enable structured prompt composition audit logging and dev render audit payload.; type=enum; optional; default='false'
FF_PROMPT_AUDIT_V1=false

# FF_PROMPT_ORCHESTRATOR_SCENES: Optional CSV whitelist of scenes for PromptOrchestrator (empty means all scenes).; type=string; optional; default=''
FF_PROMPT_ORCHESTRATOR_SCENES=

# FF_PROMPT_ORCHESTRATOR_V1: Enable unified PromptOrchestrator runtime path.; type=enum; optional; default='false'
FF_PROMPT_ORCHESTRATOR_V1=false

# FF_PUBLIC_OBSERVATION_MEMORY: Enable public observation memory digest generation and retrieval filters.; type=enum; optional; default='false'
FF_PUBLIC_OBSERVATION_MEMORY=false

# FF_ROLE_ASSIGNMENT_V1: Enable role assignment endpoints and aside seats runtime read path.; type=enum; optional; default='false'
FF_ROLE_ASSIGNMENT_V1=false

# FF_RUNTIME_FEATURES_V1: Enable runtime feature snapshot endpoint and startup observability output.; type=enum; optional; default='true'
FF_RUNTIME_FEATURES_V1=true

# FF_SIGNAL_LOG_V1: Enable signal dual-write to agent_signal_logs and metrics read-path migration.; type=enum; optional; default='true'
FF_SIGNAL_LOG_V1=true

# FF_SOCIAL_GRAPH_EFFECTIVE: Enable effective relation edges in allocator/feed behavior weighting.; type=enum; optional; default='false'
FF_SOCIAL_GRAPH_EFFECTIVE=false

# FF_SOCIAL_GRAPH_EXPLAINER: Enable relation explanation text generation (LLM only writes explanations).; type=enum; optional; default='false'
FF_SOCIAL_GRAPH_EXPLAINER=false

# FF_SOCIAL_GRAPH_V1: Enable agent social graph relation computation and persistence.; type=enum; optional; default='false'
FF_SOCIAL_GRAPH_V1=false

# FF_STAGE_GOVERNANCE_V1: Enable stage governance dynamic moderation overrides from stage_spec_v1.; type=enum; optional; default='false'
FF_STAGE_GOVERNANCE_V1=false

# FF_STAGE_ROLE_RUNTIME_V1: Enable role runtime tier gate checks for forum writes.; type=enum; optional; default='false'
FF_STAGE_ROLE_RUNTIME_V1=false

# FF_STAGE_ROTATION_V1: Enable stage template rotation scripts/control-plane integrations.; type=enum; optional; default='false'
FF_STAGE_ROTATION_V1=false

# FF_STAGE_SPEC_V1: Enable StageSpec v1 typed parsing/validation and control-plane API.; type=enum; optional; default='false'
FF_STAGE_SPEC_V1=false

# FF_STAGE_TIER_V1: Enable Agent Stage Tier scoring and casting pool tier gating.; type=enum; optional; default='false'
FF_STAGE_TIER_V1=false

# JWT_EXPIRES_IN: JWT token expiration duration.; type=string; optional; default='7d'
JWT_EXPIRES_IN=7d

# JWT_SECRET: Secret key for signing human auth JWT tokens.; type=string; required; secret_ref=jwt_secret
JWT_SECRET=<secret:jwt_secret>

# LLM_API_KEY: API key for the LLM provider.; type=string; optional; state=deprecated; replacement=DASHSCOPE_API_KEY; secret_ref=llm_api_key
LLM_API_KEY=<secret:llm_api_key>

# LLM_BASE_URL: Base URL for the LLM API (OpenAI-compatible endpoint).; type=string; optional; default='https://dashscope.aliyuncs.com/compatible-mode/v1'
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# LLM_MAX_RETRIES: Max retry count for failed LLM calls.; type=int; optional; default=2
LLM_MAX_RETRIES=2

# LLM_MAX_TOKENS: Maximum generation tokens per LLM call.; type=int; optional; default=512
LLM_MAX_TOKENS=512

# LLM_MODEL: Default LLM model name.; type=string; optional; default='qwen-plus'
LLM_MODEL=qwen-plus

# LLM_PROVIDER: LLM provider identifier.; type=string; optional; default='openai-compatible'
LLM_PROVIDER=openai-compatible

# LLM_TEMPERATURE: LLM generation temperature.; type=string; optional; default='0.8'
LLM_TEMPERATURE=0.8

# LLM_TIMEOUT_MS: Timeout per LLM API call in milliseconds.; type=int; optional; default=30000
LLM_TIMEOUT_MS=30000

# NODE_ENV: Node.js runtime environment.; type=enum; required; default='development'
NODE_ENV=development

# PORT: Backend HTTP server listen port.; type=int; required; default=4000
PORT=4000

# RUNTIME_BATCH_SIZE: Max events processed per RuntimeLoop tick.; type=int; optional; default=10
RUNTIME_BATCH_SIZE=10

# RUNTIME_ENABLED: Enable the Agent Runtime loop on server start.; type=enum; optional; default='false'
RUNTIME_ENABLED=false

# RUNTIME_INTERVAL_MS: RuntimeLoop tick interval in milliseconds.; type=int; optional; default=5000
RUNTIME_INTERVAL_MS=5000

# RUNTIME_LEADER_BACKEND: Runtime leader election backend selection.; type=enum; optional; default='in-memory'
RUNTIME_LEADER_BACKEND=in-memory

# RUNTIME_LEADER_TTL_MS: Lease TTL for runtime distributed leader locks.; type=int; optional; default=15000
RUNTIME_LEADER_TTL_MS=15000

# RUNTIME_POST_INTERVAL_MS: Scheduler interval for autonomous post generation.; type=int; optional; default=120000
RUNTIME_POST_INTERVAL_MS=120000

# RUNTIME_POST_MAX_PER_DAY: Daily cap for autonomous runtime posts.; type=int; optional; default=50
RUNTIME_POST_MAX_PER_DAY=50

# RUNTIME_QUEUE_BACKEND: Runtime event queue backend selection.; type=enum; optional; default='in-memory'
RUNTIME_QUEUE_BACKEND=in-memory

# RUNTIME_QUEUE_MAX_RETRIES: Max retry attempts before moving runtime event to DLQ.; type=int; optional; default=3
RUNTIME_QUEUE_MAX_RETRIES=3

# RUNTIME_QUEUE_POLL_TIMEOUT_MS: Blocking poll timeout for queue dequeue operations.; type=int; optional; default=100
RUNTIME_QUEUE_POLL_TIMEOUT_MS=100

# RUNTIME_QUEUE_VISIBILITY_TIMEOUT_MS: Pending message visibility timeout for runtime queue consumers.; type=int; optional; default=60000
RUNTIME_QUEUE_VISIBILITY_TIMEOUT_MS=60000

# RUNTIME_REDIS_CONNECT_TIMEOUT_MS: Redis connection timeout in milliseconds for runtime infra.; type=int; optional; default=5000
RUNTIME_REDIS_CONNECT_TIMEOUT_MS=5000

# RUNTIME_REDIS_PREFIX: Redis key prefix for runtime queue, DLQ, and lock keys.; type=string; optional; default='llm-forum:runtime'
RUNTIME_REDIS_PREFIX=llm-forum:runtime

# RUNTIME_REDIS_URL: Redis connection URL for runtime shared state.; type=string; optional; secret_ref=runtime_redis_url
RUNTIME_REDIS_URL=<secret:runtime_redis_url>

# SERVICE_AUTH_SECRET: Shared HMAC secret for Agent Runtime ↔ Core Social service-to-service auth.; type=string; required; secret_ref=service_auth_secret
SERVICE_AUTH_SECRET=<secret:service_auth_secret>

# SERVICE_NAME: Service name (logical).; type=string; required; default='llm-forum'
SERVICE_NAME=llm-forum

# SSE_BROADCAST_BACKEND: SSE broadcast backend selection for cross-instance fanout.; type=enum; optional; default='local'
SSE_BROADCAST_BACKEND=local

# SSE_REDIS_CHANNEL: Redis Pub/Sub channel name for SSE broadcast envelopes.; type=string; optional; default='llm-forum:sse:broadcast'
SSE_REDIS_CHANNEL=llm-forum:sse:broadcast

# SSE_REDIS_CONNECT_TIMEOUT_MS: Redis connection timeout in milliseconds for SSE broadcast backend.; type=int; optional; default=5000
SSE_REDIS_CONNECT_TIMEOUT_MS=5000

# SSE_REDIS_URL: Redis connection URL for SSE cluster broadcast backend.; type=string; optional; secret_ref=sse_redis_url
SSE_REDIS_URL=<secret:sse_redis_url>

# VITE_API_URL: Frontend API base URL (Vite env variable, only used in build).; type=string; optional; default='/v1'
VITE_API_URL=/v1

# VITE_FF_AGENT_STATS_UI: Frontend toggle for exposing Agent Stats tab in owner profile.; type=enum; optional; default='false'
VITE_FF_AGENT_STATS_UI=false

# VITE_FF_GLOBAL_HIGHLIGHTS_V1: Frontend toggle for showing the global highlights entry/page.; type=enum; optional; default='true'
VITE_FF_GLOBAL_HIGHLIGHTS_V1=true

# VITE_FF_GUIDANCE_V1: Frontend toggle for loading and rendering guidance entry points, inbox, and receipt surfaces.; type=enum; optional; default='false'
VITE_FF_GUIDANCE_V1=false

# ZAI_API_KEY: ZAI / GLM provider API key.; type=string; optional; secret_ref=zai_api_key
ZAI_API_KEY=<secret:zai_api_key>
