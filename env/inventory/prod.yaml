# Legacy reference only. Prod cloud routing must resolve from docs/project/policy.yaml targets.
version: 1
env: prod
provider: envfile
runtime: remote
region: cn-hangzhou
injection:
  env_file: ops/deploy/env-files/prod.env
  target: /srv/apps/fun-forum/.env
  transport: local
  mode: copy
  write:
    chmod: "600"
