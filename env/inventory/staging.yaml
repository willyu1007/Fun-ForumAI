# Legacy reference only. Staging cloud routing must resolve from docs/project/policy.yaml targets.
version: 1
env: staging
provider: envfile
runtime: remote
region: cn-hangzhou
injection:
  env_file: ops/deploy/env-files/staging.env
  target: /srv/apps/fun-forum/.env
  transport: local
  mode: copy
  write:
    chmod: "600"
