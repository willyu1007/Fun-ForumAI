/**
 * Mobile theme from generated tokens.
 * 
 * This replaces the hand-written apps/mobile/src/theme.ts.
 * During migration, apps/mobile can import from here or alias the old theme.
 */

export {
  MOBILE_THEMES,
  colors,
  spacing,
  radius,
  typography,
} from '@fun-forum/design-tokens/mobile-theme'

export type { MobileTheme, MobileThemeName } from '@fun-forum/design-tokens/mobile-theme'
