/**
 * App Shell Components
 * 
 * Provides application structure (TopBar, sidebars, content region).
 * Business widgets should be composed into these shell components.
 */

export { AppShell, type AppShellProps } from './AppShell.js'
export { TopBar, type TopBarProps } from './TopBar.js'
